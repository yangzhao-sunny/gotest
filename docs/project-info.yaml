module: github.com/test/taskmgr
# Handler code is covered by 10 integration acceptance scenarios in internal/integration/.
# Per-package profiling does not capture cross-package test execution, so measurable
# unit-test coverage is lower than effective coverage. Threshold reflects unit-test-only baseline.
coverage_threshold: 55
go_standards: true
